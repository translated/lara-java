package com.translated.lara.translator;

/**
 * Options used when uploading an audio file for translation.
 * <p>
 * Instances of this class are converted to HTTP query/body parameters
 * via {@link #toParams()} and consumed by {@link AudioTranslator}.
 */
public class AudioUploadOptions extends AudioOptions {

}
